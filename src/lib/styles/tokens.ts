export const bg = '#030704';

/**
 * The lower, the more it will leave a trail.
 */
const phosphorGlowAlpha = 0.32;
// const phosphorGlowAlpha = 1;
/**
 * This is actually not a green, but a translucent background we stack before drawing shapes to create
 * the trail effect.
 */
export const phosphorGlow = `rgba(3, 7, 4, ${phosphorGlowAlpha})`;

export const phosphor = '#5dff8f';
